import SwiftUI

extension Color {
    // Color de marca de la app
    static let brand      = Color(red: 0.01, green: 0.53, blue: 0.45)   // teal esmeralda
    static let brandDark  = Color(red: 0.04, green: 0.11, blue: 0.10)   // fondo oscuro con tinte teal

    // Resultado
    static let win  = Color(red: 0.13, green: 0.73, blue: 0.36)
    static let draw = Color(red: 0.95, green: 0.62, blue: 0.07)
    static let loss = Color(red: 0.93, green: 0.26, blue: 0.26)
}
